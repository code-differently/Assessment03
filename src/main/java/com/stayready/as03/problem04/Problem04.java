package com.stayready.as03.problem04;


public class Problem04 {
  
    public Boolean harmlessRansomNote(String message , String magazineText){
        /*
        i = 0;
        int len1 = message.length();
        int len2 = magazineText.length();

        if(len1 != len2)
        return false;

        while (i < len1)
        if (message.charAt(i) == magazineText.charAt(i))
        {
            i++;
        }
        
        else if (!((message.charAt(i) >= "" && message.charAt(i) <= "") 
        || (message.charAt(i) >= " " && message.charAt(i) <= " ")))
        {
            return false;
        }
        else if (!((magazineText.charAt(i) >= "" && magazineText.charAt(i) <= " ") 
        || (magazineText.charAt(i) >= " " && magazineText.charAt(i) <= " ")))
        {
            return false;
        }
        */
        return false;
        
    }
    
    
}
